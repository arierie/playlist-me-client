import React from 'react';
import IndexView from '../views/IndexView'

class IndexController extends React.Component {
    
    render() {
        return (
            <IndexView>
                <btn-connect onClick={null} />
            </IndexView>
        )
    };
}

export default IndexController;
